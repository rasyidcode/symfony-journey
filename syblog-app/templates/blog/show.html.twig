{% extends 'base.html.twig' %}

{% block title %} {{ post.title }} {% endblock %}

{% block body %}
    <h1>{{ post.title }}</h1>
    <div class="date">{{ post.formattedCreatedAt }}</div>
    <div class="body">{{ post.body }}</div>
{% endblock %}